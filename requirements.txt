# Core runtime (offline-capable defaults). All optional deps are guarded in code.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9

# Document intelligence (optional but recommended). The pipeline degrades
# gracefully if these are missing.
pypdf>=4.0

# Optional cloud AI providers. Not required for the offline default backend.
# openai>=1.30
# anthropic>=0.25

# Dev / testing
pytest>=8.0
